# Copy into your MOBILE app project (Expo / React Native), NOT into Symfony .env.
# Rename to .env in the mobile repo and restart Metro/Expo.

# Production (Railway) — use this on a real phone without USB:
EXPO_PUBLIC_API_URL=https://web-production-6bdab.up.railway.app

# Local dev only (PC running php -S 127.0.0.1:8000 + adb reverse):
# EXPO_PUBLIC_API_URL=http://127.0.0.1:8000
